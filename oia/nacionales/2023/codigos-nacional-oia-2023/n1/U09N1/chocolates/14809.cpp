#include <string>
#include <vector>

using namespace std;

int chocolates(int K, vector<int> &x) {
	vector <int> v;
    for (int i=0; i < x.size(); ++i){
		  if (v ! x){
			   v.push_back;
		  }			
	    }
	    return v.push_back
	 }

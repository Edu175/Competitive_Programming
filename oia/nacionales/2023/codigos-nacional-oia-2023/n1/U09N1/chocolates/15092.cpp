#include <bits/stdc++.h>

using namespace std;

int chocolates(int K, vector<int> &x) {
	vector <int> v;
		for (int i=0; i<x.sise(); ++i){
		string n = to_string(x[i]);
		string n = to_string(k);
		  if (n[i] ! n[n.size() -1){
			  v.push_back(x[i]);
		  }
	  }
	  return v.size();
	
}

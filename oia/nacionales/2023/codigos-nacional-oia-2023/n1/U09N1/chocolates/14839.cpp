#include <string>
#include <vector>

using namespace std;

int chocolates(int K, vector<int> &x) {
	vector <bool> v;
	int result;
    for (int i=0; i < x.size(); ++i){
			string n = to_string(x[i]);
			string n = to_string(v[i]);
			if (x[i] == v[i]){
				result.push_back(x[i] + v[i]);
				
    }
	    return result.size
}

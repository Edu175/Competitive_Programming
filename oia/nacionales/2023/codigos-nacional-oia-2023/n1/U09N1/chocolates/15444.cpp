#include <iostream>
#include <string>
#include <vector>

using namespace std;
int caja;
int chocolates(int K, vector<int> &x){
	vector <bool> v;
	int total;
          for (int i=0; i<x.size(); ++i){
			      if (K > x.size()){
					    caja = caja + K;
					        if (K == n){
								n + x.size() = total;
           				  }
			 }
			return total;
			  
}

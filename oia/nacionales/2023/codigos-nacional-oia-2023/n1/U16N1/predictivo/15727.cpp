#include <string>
#include <vector>
#include <bit/stdc++.h>

using namespace std;

int predictivo(vector<string> &r1, vector<string> &r2, vector<string> &r3, string &texto) {

  int sol=random(0,1);
  return sol;

}

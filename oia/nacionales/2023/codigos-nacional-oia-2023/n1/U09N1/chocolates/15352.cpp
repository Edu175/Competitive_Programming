#include <iostream>
#include <string>
#include <vector>

using namespace std;
int caja;
int chocolates(int K, vector<int> &x){
          for (int i=0; i<x.size() ++i){
			  caja= x.size(i);
			  if (K == x.size(i)){
				  caja;
		     }
		}
			return caja.push_back
			  
}

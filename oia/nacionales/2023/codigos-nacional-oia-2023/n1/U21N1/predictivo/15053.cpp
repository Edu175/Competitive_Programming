#include <string>
#include <vector>

using namespace std;

int predictivo(vector<string> &r1, vector<string> &r2, vector<string> &r3, string &texto)
{
    // AQUI SE DEBE IMPLEMENTAR LA SOLUCION
    int n;
    vector<string>tex1(2);
    tex1=texto=1;
    for(int i=0; i<texto.size(); i++)
    }

#include <bits/stdc++.h>
#include <string>

using namespace std;

int chocolates(int K, vector<int> &x) {
	vector <int> v;
		for (int i=0; i<x.size(); ++i){
		string n = to_string(x[i]);
		string n = to_string(k);
		  if (n[i] ! n[n.size()){
			  x.push_back(v[i]);
		  }
	  }
	  return x.size();
	
}

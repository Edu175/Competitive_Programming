#include <string>
#include <vector>

using namespace std;

vector<int> hoteles(vector<int>& padre, vector<int>& tipo, vector<int>& nodo) {
  int Q=tipo.size(), i, N;
  int P=padre.size();
  if (P>0){
      P+1;
  }
  vector<int> modo(P,-1);
  vector<int> rta(Q, 0);

  return rta;
}

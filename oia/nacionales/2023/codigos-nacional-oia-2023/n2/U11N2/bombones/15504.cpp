#include <string>
#include <vector>
#include <algorithm>
using namespace std;

long long bombones(int K, vector<int> &x) {
    long long X=0;
    int cajas=0;
    int size=x.size();
    vector<int> caja(size);

 X=size/K;


return X;
}

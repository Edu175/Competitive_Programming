#include <string>
#include <vector>

using namespace std;

int chocolates(int K, vector<int> &x) {
    // AQUI SE DEBE IMPLEMENTAR LA SOLUCION
    #include <string>
#include <vector>

using namespace std;

int chocolates(int K, vector<int> &x) {
    // AQUI SE DEBE IMPLEMENTAR LA SOLUCION
    int N,c,returnedValue;
    for(int i=0;i<N;i++){
      if(x[i]<0){
        c++;
      }
      if(c==k){
        returnedValue++;
      }
    }
    return returnedValue;
}

}
